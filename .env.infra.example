# Local infrastructure configuration
# Copy to .env.infra and adjust as needed
# Usage: docker compose -f docker-compose.infra.yml --env-file .env.infra up -d

# --- PostgreSQL ---
POSTGRES_USER=korb
POSTGRES_PASSWORD=localdev
POSTGRES_DB=korb_guru
POSTGRES_PORT=5432

# --- Qdrant ---
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334
